name: Swift

on:
  push:
    branches: [ uwu, development ]
  pull_request:
    branches: [ uwu, development ]

jobs:
  build:

    runs-on: ios-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v
